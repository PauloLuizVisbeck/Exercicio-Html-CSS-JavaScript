@model AulaEntity.Models.Compromisso

@{
    ViewData["Title"] = "Create";
    var contatosList = new SelectList(ViewBag.Contatos, "Id", "Nome");
    var locaisList = new SelectList(ViewBag.Locais, "Id", "Nome");
}

<h1>Create</h1>

<h4>Compromisso</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Local" class="control-label"></label>
                <select asp-for="LocalId" class="form-control" asp-items="@locaisList">
                    <option value="">Selecione um Local</option>
                </select>
                <span asp-validation-for="LocalId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Contato" class="control-label"></label>
                <select asp-for="ContatoId" class="form-control" asp-items="@contatosList">
                    <option value="">Selecione um Contato</option>
                </select>
                <span asp-validation-for="ContatoId" class="text-danger"></span>
            </div>
            <!-- O código da div abaixo foi elaborado pelo professor (asp-items="@ViewBag.Contatos") -->
            <!-- Esse trecho do código interage com o Create dentro do arquivo CompromissosController -->
            <!--
            <div class="form-group">
                <label asp-for="Contato" class="control-label"></label>
                <select asp-for="ContatoId" class="form-control" asp-items="@ViewBag.Contatos">
                    <option value="">Selecione um Contato</option>
                </select>
                <span asp-validation-for="ContatoId" class="text-danger"></span>
			</div>
            -->
            <div class="form-group">
                <label asp-for="Nome" class="control-label"></label>
                <input asp-for="Nome" class="form-control" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Data" class="control-label"></label>
                <input asp-for="Data" class="form-control" />
                <span asp-validation-for="Data" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
